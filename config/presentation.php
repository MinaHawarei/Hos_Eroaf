<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default presentation body font size (pixels)
    |--------------------------------------------------------------------------
    |
    | Used when the user has not set a preference in settings (cookie).
    | Session zoom controls multiply this base in the presentation UI.
    |
    */
    'default_base_font_size' => 28,

];
